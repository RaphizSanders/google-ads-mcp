/**
 * Lote account-auth: Autenticação, configurações da conta e metadados de campos.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * account-auth.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerAccountAuthTools(ctx: ToolContext): void {
  void ctx;
}
