/**
 * Lote rsa-ads: Anúncios responsivos de Pesquisa, customizadores e auditoria de anúncios.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * rsa-ads.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerRsaAdsTools(ctx: ToolContext): void {
  void ctx;
}
