/**
 * Lote reports: Relatórios de posicionamento, landing page, redes e visão do MCC.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * reports.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerReportsTools(ctx: ToolContext): void {
  void ctx;
}
