/**
 * Lote account-admin: Administração do MCC: contas, vínculos, usuários, faturamento e edição em massa.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * account-admin.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerAccountAdminTools(ctx: ToolContext): void {
  void ctx;
}
