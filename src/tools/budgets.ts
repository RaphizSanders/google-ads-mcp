/**
 * Lote budgets: Orçamentos, ritmo de gasto e grupos de campanhas.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * budgets.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerBudgetsTools(ctx: ToolContext): void {
  void ctx;
}
