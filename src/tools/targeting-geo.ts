/**
 * Lote targeting-geo: Segmentação geográfica e de idioma.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * targeting-geo.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerTargetingGeoTools(ctx: ToolContext): void {
  void ctx;
}
