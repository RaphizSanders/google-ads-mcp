/**
 * Lote experiments-tracking: Experimentos, rastreamento de URL, rótulos e aquisição de clientes.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * experiments-tracking.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerExperimentsTrackingTools(ctx: ToolContext): void {
  void ctx;
}
