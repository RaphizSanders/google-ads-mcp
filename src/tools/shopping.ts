/**
 * Lote shopping: Shopping, Merchant Center e grupos de produtos.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * shopping.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerShoppingTools(ctx: ToolContext): void {
  void ctx;
}
