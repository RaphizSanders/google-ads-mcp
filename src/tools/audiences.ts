/**
 * Lote audiences: Públicos, remarketing e Customer Match.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * audiences.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerAudiencesTools(ctx: ToolContext): void {
  void ctx;
}
