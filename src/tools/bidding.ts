/**
 * Lote bidding: Estratégias de lance, simulações, sazonalidade e datas de campanha.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * bidding.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerBiddingTools(ctx: ToolContext): void {
  void ctx;
}
