/**
 * Lote conversions-reporting: Regras de valor, detalhamento de conversões, lift e leads.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * conversions-reporting.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerConversionsReportingTools(ctx: ToolContext): void {
  void ctx;
}
