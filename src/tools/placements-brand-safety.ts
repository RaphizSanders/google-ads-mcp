/**
 * Lote placements-brand-safety: Posicionamentos, exclusões e brand safety.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * placements-brand-safety.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerPlacementsBrandSafetyTools(ctx: ToolContext): void {
  void ctx;
}
