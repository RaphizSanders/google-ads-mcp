/**
 * Lote video-display: Vídeo, YouTube e anúncios de Display.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * video-display.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerVideoDisplayTools(ctx: ToolContext): void {
  void ctx;
}
