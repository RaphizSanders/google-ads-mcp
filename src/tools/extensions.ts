/**
 * Lote extensions: Extensões (assets vinculados), WhatsApp e formulário de lead.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * extensions.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerExtensionsTools(ctx: ToolContext): void {
  void ctx;
}
