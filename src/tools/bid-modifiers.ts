/**
 * Lote bid-modifiers: Ajustes de lance, programação e demografia.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * bid-modifiers.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerBidModifiersTools(ctx: ToolContext): void {
  void ctx;
}
