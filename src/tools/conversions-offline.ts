/**
 * Lote conversions-offline: Importação offline, ajustes de conversão, chamadas e GCLID.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * conversions-offline.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerConversionsOfflineTools(ctx: ToolContext): void {
  void ctx;
}
