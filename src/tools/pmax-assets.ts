/**
 * Lote pmax-assets: Performance Max: criação e gestão de asset groups.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * pmax-assets.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerPmaxAssetsTools(ctx: ToolContext): void {
  void ctx;
}
