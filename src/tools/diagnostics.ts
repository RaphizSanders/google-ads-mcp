/**
 * Lote diagnostics: Diagnóstico de veiculação, parcela de impressões e reprovações.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * diagnostics.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerDiagnosticsTools(ctx: ToolContext): void {
  void ctx;
}
