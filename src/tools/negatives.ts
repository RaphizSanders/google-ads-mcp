/**
 * Lote negatives: Palavras-chave negativas em todos os níveis.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * negatives.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerNegativesTools(ctx: ToolContext): void {
  void ctx;
}
