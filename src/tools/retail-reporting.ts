/**
 * Lote retail-reporting: Relatórios de varejo, status de produtos, canais do PMax e listas de marcas.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * retail-reporting.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerRetailReportingTools(ctx: ToolContext): void {
  void ctx;
}
