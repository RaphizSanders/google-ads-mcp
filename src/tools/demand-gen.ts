/**
 * Lote demand-gen: Demand Gen e remarketing dinâmico de varejo em Display.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * demand-gen.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerDemandGenTools(ctx: ToolContext): void {
  void ctx;
}
