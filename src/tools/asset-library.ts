/**
 * Lote asset-library: Biblioteca de assets e locais do Perfil da Empresa.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * asset-library.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerAssetLibraryTools(ctx: ToolContext): void {
  void ctx;
}
