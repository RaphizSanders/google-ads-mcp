/**
 * Lote keywords: Palavras-chave, termos de pesquisa e DSA.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * keywords.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerKeywordsTools(ctx: ToolContext): void {
  void ctx;
}
