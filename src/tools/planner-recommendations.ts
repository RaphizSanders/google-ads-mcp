/**
 * Lote planner-recommendations: Planejador de palavras-chave e recomendações.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * planner-recommendations.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerPlannerRecommendationsTools(ctx: ToolContext): void {
  void ctx;
}
