/**
 * Lote pmax-signals: Performance Max: sinais, automação, marca, prévias e combinações.
 * Registre as tools novas com ctx.mcp.registerTool e classifique cada uma em
 * pmax-signals.catalog.ts. Tools de leitura precisam chamar checkCustomerAccess (o teste de
 * allowlist confere no código-fonte).
 */
import type { ToolContext } from "../tool-kit.js";

export function registerPmaxSignalsTools(ctx: ToolContext): void {
  void ctx;
}
