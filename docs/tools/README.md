# Documentação das tools por área

Uma página por módulo de `src/tools/` (o nome do arquivo é o grupo usado em
`GOOGLE_ADS_TOOL_GROUPS`): tools novas e alteradas, parâmetros, fluxos, limitações e o que ficou
parcial e por quê. O índice com todas as tools está no README principal, em "Tools por área".
